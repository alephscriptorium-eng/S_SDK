{
  "name": "script-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "SCRIPT_SDK — workspace raíz del holón 07 (web VitePress + anclas HOLONES)",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "docs:sync-ensayo": "node scripts/sync-ensayo.mjs",
    "docs:dev": "npm run docs:sync-ensayo && vitepress dev docs",
    "docs:build": "npm run docs:sync-ensayo && vitepress build docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "license": "UNLICENSED"
}
