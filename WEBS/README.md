# WEBS/ — instancia de capa de contenido (holón 07)

**Método:** skill `site-web` en
`S_SDK-skills-library` @ `019a90b` / paquete `@alephscript/skills-scriptorium@0.1.0`
(`skills/site-web/`). Procedencia del método: plantillas y
`reference/metodo-*.md` del skill (DE-I6 · DE-I10). **No** se copia el
árbol de datos de otros mundos.

**Datos (esta carpeta):** calibración + bases + CANTERA + ENTREGA-CAPA-*
del portal `s-sdk.escrivivir.co` (este repo). Separación skill↔instancia.

```
WEBS/
  CALIBRACION.md          parámetros mundo.*
  BASE-1-ARGUMENTO.md     qué se dice
  BASE-2-SISTEMA.md       de qué está hecho
  BASE-3-MECANISMO.md     cómo se genera (apunta al skill §B–§E)
  CANTERA/                inventarios locales; se consultan, no dirigen
  ENTREGA-CAPA-1/         paquete vigente capa-1 (activación)
```

`docs/` del sitio vive en la raíz del repo (VitePress ya montado en I1).
Esta instancia **no** duplica el portal: genera / entrega copy hacia él
vía paquetes §E. Portada + fichas de holones = olas I31/I32
(`<pendiente>`).

## Activación (capa-1)

1. Leer `CALIBRACION.md` y las tres BASES.
2. Consultar `CANTERA/` solo por datos.
3. Aplicar `ENTREGA-CAPA-1/` cuando el swarm lo asigne (formato §E).
4. Iteración de backtracking: ver skill `site-web` SKILL.md §B.
